# Optional — the server already defaults to the same public/publishable
# Supabase project used by ../supabase.js (read-only access via RLS).
# Override only if you point this at a different project.
SUPABASE_URL=https://pyiukqeuaxonsrrgbfzq.supabase.co
SUPABASE_KEY=sb_publishable_MvaYVraw3FqViy9G4nhKSQ_QbFpMudx
